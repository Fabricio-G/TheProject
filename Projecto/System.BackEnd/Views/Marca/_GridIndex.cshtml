@model IEnumerable<System.Services.Models.MarcaViewModel>
@{
    var Email = User.Claims.FirstOrDefault(x => x.Type == "Usuario").Value;
    var edit = Html.ColumnTablePermission("Edit", "Marca", Email);
    var delete = Html.ColumnTablePermission("Delete", "Marca", Email);
    ViewData["Title"] = "Marcas";
}
@(Html.Grid(Model)
            .Build(columns =>
            {
                columns.Add(m => m.MarcaId).Titled("N°");
                columns.Add(m => m.Nombre).Titled("Nombre");
                columns.Add(m => Html.ActionLink("", "Edit", "Marca", new { Id = m.MarcaId }, new { @class = "zmdi zmdi-edit text-warning", @title = "Editar" })).Css("action-cell " + edit).Titled("Editar");
                columns.Add(m => Html.ActionLink("", "Delete", "Marca", new { Id = m.MarcaId }, new { @class = "zmdi zmdi-delete text-danger swal-delete", @title = "Eliminar" })).Css("action-cell " + delete).Titled("Eliminar");
            })
            .Empty("No se encontró ningún registro")
            .Css("table table-hover")
)
